// Dedicated Vercel route for /api/tickets. All route behavior is implemented in
// src/server/routes/tickets.js and mounted by the shared Express app.
const app = require('../src/server/app');

module.exports = app;
